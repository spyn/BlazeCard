# Optional real Apple signing certs

Final tab **preview mode** (default) does **not** need certificates.

Only place files here if you set `BlazeCard:PkPass:UsePreviewCertificates=false`
and want PassGenerator to CMS-sign with a real Pass Type ID `.p12` + Apple WWDR `.cer`.
